<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novedu.nov.statistics.mapper.StatisticsDailyMapper">

    <select id="statisticsAWeekUserRegisterAndLoginCount" resultType="StatisticsDaily">
        select * from statistics_daily
        where date(create_time) > DATE_SUB(CURDATE(), INTERVAL 8 DAY) and date(create_time)!=(CURDATE()) ORDER BY create_time;
    </select>
</mapper>
